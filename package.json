{
  "name": "fastify-system-prompt-proxy",
  "version": "2.0.0",
  "description": "Minimal Fastify proxy for system prompt replacement",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "fastify": "^4.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "pino-pretty": "^13.1.2"
  }
}